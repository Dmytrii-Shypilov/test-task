import s from './job.scss'
import { useParams } from 'react-router-dom'

const JobPage = () => {

const {id} = useParams()

    return(
        <section>

        </section>
    )
}

export default JobPage